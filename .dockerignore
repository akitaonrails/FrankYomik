# VCS
.git/
.gitignore

# Documentation
*.md
docs/

# Flutter client (separate build)
client/

# Tests and test data
tests/
output/

# Python artifacts
.venv/
server/.venv/
__pycache__/
*.pyc
*.pyo
*.egg-info/
.pytest_cache/

# IDE / editor
.claude/
.vscode/
.idea/
*.swp
*.swo

# Font sources (only need .ttf/.otf for runtime)
server/fonts/comic-neue.zip
server/fonts/komika.zip
server/fonts/badaboom.zip
server/fonts/comicneue-master/
server/fonts/komika_text/
server/fonts/badaboom/
server/fonts/genei-mirror/

# Ollama models (mounted at runtime, not baked into image)
server/models/

# Disk cache (mounted at runtime)
cache/
server/cache/

# Compiled Go binaries
server/server

# Webtoon data / scratch
webtoon_data/
